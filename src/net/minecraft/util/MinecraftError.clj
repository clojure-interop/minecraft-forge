(ns net.minecraft.util.MinecraftError
  (:refer-clojure :only [require comment defn ->])
  (:import [net.minecraft.util MinecraftError]))

(defn ->minecraft-error
  "Constructor."
  (^MinecraftError []
    (new MinecraftError )))

