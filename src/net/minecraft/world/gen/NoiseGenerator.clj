(ns net.minecraft.world.gen.NoiseGenerator
  (:refer-clojure :only [require comment defn ->])
  (:import [net.minecraft.world.gen NoiseGenerator]))

(defn ->noise-generator
  "Constructor."
  (^NoiseGenerator []
    (new NoiseGenerator )))

