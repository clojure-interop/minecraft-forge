(ns net.minecraft.client.model.ModelCow
  (:refer-clojure :only [require comment defn ->])
  (:import [net.minecraft.client.model ModelCow]))

(defn ->model-cow
  "Constructor."
  (^ModelCow []
    (new ModelCow )))

