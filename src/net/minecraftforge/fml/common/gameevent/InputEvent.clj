(ns net.minecraftforge.fml.common.gameevent.InputEvent
  (:refer-clojure :only [require comment defn ->])
  (:import [net.minecraftforge.fml.common.gameevent InputEvent]))

(defn ->input-event
  "Constructor."
  (^InputEvent []
    (new InputEvent )))

