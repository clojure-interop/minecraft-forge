(ns net.minecraftforge.fml.common.network.internal.FMLMessage$EntityMessage
  (:refer-clojure :only [require comment defn ->])
  (:import [net.minecraftforge.fml.common.network.internal FMLMessage$EntityMessage]))

(defn ->entity-message
  "Constructor."
  (^FMLMessage$EntityMessage []
    (new FMLMessage$EntityMessage )))

