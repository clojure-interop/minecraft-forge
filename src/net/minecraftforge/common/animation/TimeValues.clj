(ns net.minecraftforge.common.animation.TimeValues
  "Various implementations of ITimeValue."
  (:refer-clojure :only [require comment defn ->])
  (:import [net.minecraftforge.common.animation TimeValues]))

(defn ->time-values
  "Constructor."
  (^TimeValues []
    (new TimeValues )))

