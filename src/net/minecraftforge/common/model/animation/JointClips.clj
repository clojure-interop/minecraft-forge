(ns net.minecraftforge.common.model.animation.JointClips
  "Various implementations of IJointClip."
  (:refer-clojure :only [require comment defn ->])
  (:import [net.minecraftforge.common.model.animation JointClips]))

(defn ->joint-clips
  "Constructor."
  (^JointClips []
    (new JointClips )))

