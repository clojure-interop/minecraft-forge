(ns net.minecraftforge.common.util.JsonUtils
  (:refer-clojure :only [require comment defn ->])
  (:import [net.minecraftforge.common.util JsonUtils]))

(defn ->json-utils
  "Constructor."
  (^JsonUtils []
    (new JsonUtils )))

