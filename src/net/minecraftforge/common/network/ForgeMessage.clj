(ns net.minecraftforge.common.network.ForgeMessage
  (:refer-clojure :only [require comment defn ->])
  (:import [net.minecraftforge.common.network ForgeMessage]))

(defn ->forge-message
  "Constructor."
  (^ForgeMessage []
    (new ForgeMessage )))

