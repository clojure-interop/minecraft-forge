(ns net.minecraftforge.gradle.GradleStartCommon
  (:refer-clojure :only [require comment defn ->])
  (:import [net.minecraftforge.gradle GradleStartCommon]))

(defn ->gradle-start-common
  "Constructor."
  (^GradleStartCommon []
    (new GradleStartCommon )))

